spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8097
      label: feat/config
      profile: prod
      name: user-service, shared
  rabbitmq:
    host: devenv-aws.invoicefair.club
    port: 35764
    username: invoicefair
    password: oyTDOLZg9JOK

management:
  endpoint:
    shutdown:
      enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
