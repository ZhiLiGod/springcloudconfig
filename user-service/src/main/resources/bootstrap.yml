spring:
  profiles:
    active: dev
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        prefix: config
        enabled: true
        format: YAML
        data-key: data
        default-context: shared
  application:
    name: user-service