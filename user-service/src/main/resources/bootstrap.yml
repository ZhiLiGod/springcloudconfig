spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8097
      label: feat/config
      profile: prod
      name: user-service, shared
management:
  endpoint:
    shutdown:
      enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
